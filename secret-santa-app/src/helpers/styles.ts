import { Box, styled } from "@mui/material";
import { MainButton } from "./uiHelpers";

export  const StyledBox = styled(Box)({
    backgroundColor: '#fff',
    padding: '20px',
    borderRadius: '10px',
  });

export  const MainButton1 = styled(MainButton)({
    margin: '10px',

  });